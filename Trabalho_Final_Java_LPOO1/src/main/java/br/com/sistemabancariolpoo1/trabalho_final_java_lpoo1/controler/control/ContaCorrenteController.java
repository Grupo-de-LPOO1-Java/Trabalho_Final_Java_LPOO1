/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package br.com.sistemabancariolpoo1.trabalho_final_java_lpoo1.controler.control;

/**
 *
 * @author joaop
 */
public class ContaCorrenteController {
    //private SistemaBanco sys = new SystemaBanco();
    //private ContaCorrenteDao modelDao = ContaCorrenteDaoSql.getContaDaoSql(); // Exemplo de acesso ao DAO
    //private View view; // Exemplo de ligação com a view


public void criarContaCorrente() {
       /* try{
            ContaCorrente conta = view.getContatoFormulario();
            modelDao.add(conta);
            view.inserirContaCorrenteView(conta);
            view.apresentaInfo("Adicionado com sucesso!!!");
            
        }catch(Exception ex){
            view.apresentaErro("Erro ao criar conta corrente.");
        }*/
       
}
 public void sacarContaCorrente() {
        /*try {
            int numeroConta = view.solicitarNumeroConta();
            double valor = view.solicitarValorSaque();
            ContaCorrente conta = modelDao.getById(numeroConta);

            if (conta.saca(valor)) {
                modelDao.update(conta);
                view.apresentaInfo("Saque realizado com sucesso!");
            } else {
                view.apresentaErro("Saldo insuficiente para o saque.");
            }
        } catch (Exception ex) {
            view.apresentaErro("Erro ao sacar da conta corrente.");
        }*/
    }

    public void depositarContaCorrente() {
     /*   try {
            int numeroConta = view.solicitarNumeroConta();
            double valor = view.solicitarValorDeposito();
            ContaCorrente conta = modelDao.getById(numeroConta);

            conta.deposita(valor);
            modelDao.update(conta);
            view.apresentaInfo("Depósito realizado com sucesso!");
        } catch (Exception ex) {
            view.apresentaErro("Erro ao depositar na conta corrente.");
        }*/
    }

    public void verSaldoContaCorrente() {
        /*try {
            int numeroConta = view.solicitarNumeroConta();
            ContaCorrente conta = modelDao.getById(numeroConta);
            view.apresentarSaldo(conta.getSaldo());
        } catch (Exception ex) {
            view.apresentaErro("Erro ao consultar saldo da conta corrente.");
        }
    }
}*/
}
    public void RemunerarContaCorrente(){
//colocar método aqui
}
}

